# 15. 코드 구조 다이어그램

Type: task
Status: claimed
Blocked by: (없음)

## Question

사용자가 `archify` 스킬을 지정해 코드 구조를 정리해달라고 했다.
앞으로 **"코드 구조 정리하자"** 는 말이 나오면 이 스킬을 부른다 (전역 메모리에 기록함).

그릴 것: 수집 → 관련성 → 묶음 → 화제도 → 섹션 조립 → 보드 JSON → PWA,
그리고 **벤더 채널 분기**(클로즈드는 자기 페이지 / 오픈웨이트는 HF)와
**예산 게이트**(fail-closed).

산출물: `docs/architecture/pipeline.dataflow.json` → 단독 HTML.
showcase 품질 검증을 통과해야 한다.

## Answer

(진행 중 — 레이아웃 진단 수리 중)
